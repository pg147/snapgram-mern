export type INewUser = {
    name: string,
    email: string,
    password: string,
    username: string
}

export type IUser = {
    id: string;
    name: string;
    username: string;
    email: string;
    imageURL: string;
    bio: string;
  };